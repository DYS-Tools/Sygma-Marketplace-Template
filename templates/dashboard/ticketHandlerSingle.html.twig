{% extends 'base.html.twig' %}


{% block title %}Dashboard{% endblock %}

{% block body %}
    <!-- display products that customers have purchased, available for download -->

    {{ include('dashboard/_nav.html.twig') }}

    <div class="container">
        <br/><br/>
        <h2> Dashboard {{ user.email }} </h2>
        <br/>


        <h2> Le ticket n°{{ ticket.id }} </h2>
        <br/>
        <hr>
        {{ ticket.name }}<br/>
        {{ticket.subject }} </br>
        {{ ticket.message }}</br>
        {{ticket.status }}</br>

        {% if ticket.status == 0 %}
         <p> Ce ticket ne possède pas encore de réponse </p>

         {{ form_start(form) }}
            {{ form_widget(form) }}
            <a type="submit"><button class="btn"> Envoyer</button></a>
        {{ form_end(form) }}

        {% endif %}

    </div>

{% endblock %}
