{% extends 'base.html.twig' %}

{% block title %}Nos produits{% endblock %}

{% block body %}

<ul class="nav nav-pills justify-content-center category-nav">
    {% for category in categories %}
    <li class="nav-item">
        <a class="nav-link category-link-nav" href="{{ path('product_with_category', {'id': category.id}) }}">{{ category.name }}</a>
    </li>
    {% endfor %}
</ul>

    <img src="img/head.jpg" alt="Web-Item-Market" style="width: 100%">
<br/>
<br/>

<!-- nEW SEARCH BAR --> 

<div class="container search-container">
{{ form_start(searchForm) }}
    {{ form_widget(searchForm) }}
    <button class="btn-success product-button">{{ button_label|default('Rechercher') }}</button>
{{ form_end(searchForm) }}
</div>

    <!-- -------------------------------------------
                container-best-sell
    -------------------------------------------  -->
    <div class="container-best-sell col-lg-12">
        <div class="box-card">
            <h2 class="text-left"><i class="fa fa-file-text" aria-hidden="true"></i>  Nos meilleurs produits  </h2>
            <div class="row">    
                {% for product in pagination %}
                    <div class="col-lg-3">
                        <!-- product card -->
                        <div class="card" style="width: 26rem;">
                            <img class="card-img-top" src="product/img/{{ product.img1 }}"  alt="{{ product.name }}">
                            <div class="card-body">
                                <h5 class="card-title-product">{{ product.name }}</h5>
                                <p class="card-text">{{ product.description }}</p>

                                <p class="yellowgreen"> <strong> {{ product.price }} € </strong>  </p>

                                <div class="row">
                                    <div class="col-lg-8">
                                        <p class="card-text"> {{ product.numberSale}}  Vente </p>
                                    </div>

                                    <div class="col-lg-4">
                                        <a href="{{ path('product_show', {'id': product.id}) }}" class="btn btn-info"><i class="fa fa-eye" aria-hidden="true"></i></a>
                                        <a href="{{path('order', { 'product': product.id }  ) }}" class="btn btn-info"><i class="fa fa-shopping-cart" aria-hidden="true"></i>  </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!-- end product card -->
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
    <!-- end container-best-sell -->


    <!-- -------------------------------------------
            container-product-new
    -------------------------------------------  -->
    <div class="container-product-new col-lg-12">
        <div class="box-card">
            <h2 class="text-left"><i class="fa fa-file-text" aria-hidden="true"></i>  Nouveau produits  </h2>
            <div class="row">
                {% for product in pagination %}
                    <div class="col-lg-3">
                        <!-- product card -->
                        <div class="card" style="width: 26rem;">
                            <img class="card-img-top" src="product/img/{{ product.img1 }}"  alt="{{ product.name }}">
                            <div class="card-body">
                                <h5 class="card-title-product">{{ product.name }}</h5>
                                <p class="card-text">{{ product.description }}</p>

                                <p class="yellowgreen"> <strong> {{ product.price }} € </strong>  </p>

                                <div class="row">
                                    <div class="col-lg-8">
                                        <p class="card-text"> {{ product.numberSale}}  Vente </p>
                                    </div>

                                    <div class="col-lg-4">
                                        <a href="{{ path('product_show', {'id': product.id}) }}" class="btn btn-info"><i class="fa fa-eye" aria-hidden="true"></i></a>
                                        <a href="{{path('order', { 'product': product.id }  ) }}" class="btn btn-info"><i class="fa fa-shopping-cart" aria-hidden="true"></i>  </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!-- end product card -->
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>



    <!-- -------------------------------------------
                Free Template
    -------------------------------------------  -->
    <div class="container-free col-lg-12">
        <div class="box-card">
            <h2 class="text-left"><i class="fa fa-file-text" aria-hidden="true"></i>  Template gratuit  </h2>
            <div class="row">
                {% for product in pagination %}
                    <div class="col-lg-3">
                        <!-- product card -->
                        <div class="card" style="width: 26rem;">
                            <img class="card-img-top" src="product/img/{{ product.img1 }}"  alt="{{ product.name }}">
                            <div class="card-body">
                                <h5 class="card-title-product">{{ product.name }}</h5>
                                <p class="card-text">{{ product.description }}</p>

                                <p class="yellowgreen"> <strong> {{ product.price }} € </strong>  </p>

                                <div class="row">
                                    <div class="col-lg-8">
                                        <p class="card-text"> {{ product.numberSale}}  Vente </p>
                                    </div>

                                    <div class="col-lg-4">
                                        <a href="{{ path('product_show', {'id': product.id}) }}" class="btn btn-info"><i class="fa fa-eye" aria-hidden="true"></i> </a>
                                        <a href="{{path('order', { 'product': product.id }  ) }}" class="btn btn-info"><i class="fa fa-shopping-cart" aria-hidden="true"></i>  </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!-- end product card -->
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
    <!-- container-free -->

    <!-- container-product-new -->

        {# total items count #}

    <!-- <div class="count">
        { { pagination.getTotalItemCount }}
    </div>
    -->

    {# display navigation #}
    <div class="navigation navigation-block">
        {{ knp_pagination_render(pagination) }}
    </div>

</div>
{% endblock %}
